import { ReactNode } from "react";

export const metadata = {
  title: "Shop - Explore Our Products",
  description:
    "Browse a wide variety of high-quality products tailored to your needs.",
};
export default function Layout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
